[project]
name = "mpp-core"
version = "0.1.0"
description = "Marketplace Parser Core modular MVP"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"
